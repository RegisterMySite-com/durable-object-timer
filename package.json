{
  "name": "global-timer",
  "version": "1.0.0",
  "private": true,
  "description": "Persistent global clock timer using Cloudflare Workers + Durable Objects",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260823.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.125.0"
  }
}
